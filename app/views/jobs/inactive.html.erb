<h1>Inactive Jobs</h1>

<table class= "table table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Command</th>
      <th>Times to Run</th>
      <th>Cron Input</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
    <tr>
      <td><%= job.title %></td>
      <td><%= job.command %></td>
      <td><%= job.times_to_run %></td>
      <td><%= job.cron_input %></td>
      <td><%= button_to 'Restart', {controller: "jobs", action: "restart", job: job}, class: "btn btn-sm btn-default"%></td>
    </tr>
    <% end %>
  </tbody>
</table>

<br>